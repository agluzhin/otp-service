# Server
server.port=8080

# Database
db.url=jdbc:postgresql://localhost:5432/otpservice
db.username=postgres
db.password=postgres
db.pool.size=10

# JWT
jwt.secret=change-me-to-a-long-random-secret-at-least-256-bits
jwt.expiration.seconds=3600

# OTP expiration scheduler interval (seconds)
scheduler.interval.seconds=60